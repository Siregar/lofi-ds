export type IsMobile = {
  isMobile?: boolean;
};

export type TextLength = {
  textLength?: number;
};

export type DeviceTypes = {
  device: 'desktop' | 'mobile';
};

export type HeaderSlot = {
  headerSlot?: React.ComponentType<any>;
};

export type ActiveItem = {
  activeItemIndex?: number;
};
