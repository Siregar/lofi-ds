import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="text-2xl">
      Please run `npm run storybook` and visit `http://localhost:6006/`
    </div>
  </React.StrictMode>
);
